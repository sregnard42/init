netstat -nr | grep default | head -n1 | awk '{print $2}'

# netstat -nr : show routing tables
# grep default : get only default configuration
# head -n1 : only the first result
# awk '{print $2}' : print the second value (representing the Gateway)
