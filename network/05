# nslookup slash16.org | grep Address | sed 1d | awk '{ print $2 }'
# We remove the first result of `grep Address` because it represents the IP
# of the requester

dig +short slash16.org
# gives the same result, much better, much wow

